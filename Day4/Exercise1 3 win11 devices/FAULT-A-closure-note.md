Resolved. Cause: DHCP scope for Floor 3 subnet still referenced a decommissioned DNS server, causing affected clients to receive stale DNS and fail domain controller discovery/Group Policy processing. Action: Updated DHCP Option 006 to the correct DNS server set (including 10.10.0.10), removed old DNS entries, refreshed client DHCP/DNS state, and re-ran policy processing with successful verification. Preventive: Enforce a pre-decommission DHCP Option 006 audit and dual DNS/DHCP sign-off, plus recurring scope compliance checks and event monitoring. User confirmed working.
